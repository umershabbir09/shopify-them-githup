<div class="ajax-success-compare-modal compare-page theme-default-margin" id="moda-compare">
  <div class="container">
    <div class="modal-content content" id="compare-modal">
      <div class="modal-header">
        <h4 class="modal-title">{{ settings.compare_popup_title }}</h4>
      </div>
      <div class="modal-body">
        <div class="table-wrapper">
          <table class="table table-hover table-responsive">
            <thead>
              <tr class="th-compare">
                <th></th>
              </tr>
            </thead>
            <tbody id="table-compare">
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
